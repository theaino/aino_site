package adm

import "ainosite/views"

templ Layout() {
  @views.Base() {
    { children... }
    @style()
  }
}

templ style() {
  <style>
    body {
      background-color: darkred;
      color: white;
    }
  </style>
}
